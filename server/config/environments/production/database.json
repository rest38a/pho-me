{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-bookshelf",
      "settings": {
        "client": "mysql",
        "host": "${process.env.DATABASE_HOST || '127.0.0.1'}",
        "port": "${process.env.DATABASE_PORT || 3306}",
        "database": "${process.env.DATABASE_NAME || 'admin_phome'}",
        "username": "${process.env.DATABASE_USERNAME || 'admin_phome'}",
        "password": "${process.env.DATABASE_PASSWORD || 'xCp4Gu19H0'}"
      },
      "options": {}
    }
  }
}
